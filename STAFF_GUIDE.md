# คู่มือใช้งาน HK1 Order สำหรับพนักงาน

## 1. ส่งออเดอร์

1. ไปหน้า `ส่งออเดอร์`
2. พิมพ์ชื่อสินค้า ถ้าเคยกรอกแล้วระบบจะแนะนำชื่อให้
3. เลือกขนาดถุง `200g / 250g / 500g / 1000g`
4. ใส่จำนวนถุง
5. เลือกเบอร์บด หรือเลือก `ใส่เบอร์บดเอง`
6. ใส่ชื่อลูกค้า/เลขบิล ถ้ามี
7. กด `ส่งออเดอร์`

เมื่อส่งแล้ว ออเดอร์จะบันทึกเข้า Supabase อัตโนมัติ

## 2. จัดการคิวบด

ไปหน้า `รับออเดอร์`

- ออเดอร์ใหม่: กด `เริ่มบด`
- กำลังบด: กด `ปิดออเดอร์` เมื่อทำเสร็จ
- หากลูกค้าเปลี่ยนใจหรือยกเลิก: กด `ยกเลิก`

## 3. รายงานประจำวัน

ไปหน้า `รายงาน`

1. เลือกวันที่ หรือกด `วันนี้`
2. ตรวจจำนวนออเดอร์ จำนวนถุง และเวลาบดรวม
3. กด `คัดลอกรายงาน` เพื่อส่งต่อ
4. กด `ส่งออก Excel` ถ้าต้องการไฟล์รายงาน

## 4. แจ้งปัญหา

ในหน้า `รายงาน`

1. พิมพ์ปัญหาในช่อง `แจ้งปัญหาประจำวัน`
2. กด `ส่งแจ้งปัญหา`

ปัญหาจะถูกบันทึกลง Supabase และรวมในรายงานของวันนั้น

## 5. ตั้งค่า Supabase

ไปหน้า `ตั้งค่า`

1. ใส่ `Supabase .env config`
2. กด `บันทึก`
3. กด `ซิงก์ตอนนี้` เพื่อตรวจข้อมูลล่าสุด

ถ้าเปลี่ยน Web App URL ใหม่ ให้ใส่ URL ใหม่แล้วกดบันทึก เครื่องอื่นที่เคยตั้งค่าไว้จะดึง URL ล่าสุดตามได้เอง

## หมายเหตุสำคัญ

- ถ้าออเดอร์ไม่เข้า Supabase ให้ตรวจหน้า `ตั้งค่า`
- ถ้าขึ้นว่าซิงก์มีปัญหา ให้เช็ก URL หรือแจ้งผู้ดูแล
- หลังแก้ Supabase schema ต้อง Deploy เป็น New version ทุกครั้ง
